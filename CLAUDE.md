# Project memory

## Authorship and attribution

Do not add Claude as a contributor anywhere in this repo. Concretely:

- **Commits**: always author as `Inês Leite <ines.marreiros.leite@hotmail.com>`. Set `GIT_AUTHOR_NAME`, `GIT_AUTHOR_EMAIL`, `GIT_COMMITTER_NAME`, `GIT_COMMITTER_EMAIL` on every `git commit` (env vars on the command line are fine; do not modify git config).
- **PR descriptions**: do not append "Generated by Claude Code" or any equivalent footer. If the MCP tool inserts one automatically, immediately edit the PR to remove it.
- **Co-authors**: do not add `Co-authored-by: Claude` trailers to commits.

## Branches

- Do not prefix branch names with `claude/`. Use plain descriptive names (e.g. `expand-geo-articles`, `fix-nav-spacing`).
